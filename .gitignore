# macOS
.DS_Store

# Editor
.vscode/
.idea/

# Node (in case skills.sh ever drops anything here)
node_modules/

# Install-side state — generated by skills.sh / Claude Code on the install side, not source
.agents/
.claude/skills/
skills-lock.json

# Reference material from other people's skill repos — inspiration only, not ours to publish
references/

# Local benchmark runs — not for sharing
benchmarks/
